*.gguf filter=lfs diff=lfs merge=lfs -text
*.bin filter=lfs diff=lfs merge=lfs -text
models/** filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
# Prevent the bundled dashboard HTML from dominating GitHub's
# language-detection stats (this is a Python backend project).
dashboard/index.html linguist-vendored

# Keep the offline speech-model archive out of language stats and
# diff views (it's a binary blob, not source).
models/*.zip linguist-vendored
models/*.zip -diff
