# Core — sufficient for: pip install -r requirements.txt && python -c "import main"
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6
pydantic>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0
PyYAML>=6.0
huggingface_hub>=0.14.1

# Optional (desktop). Skip on Termux/Android — no psutil wheel:
#   pip install psutil
# Speech:
#   pip install faster-whisper vosk
# PDF:
#   pip install pdfplumber pdf2image pytesseract
