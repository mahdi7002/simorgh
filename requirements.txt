# Core runtime dependencies. Exact pins are part of release reproducibility.
# AnyIO 4.15.x exposes the deprecated anyio.abc.BlockingPortal alias used by
# Starlette 1.6.0 TestClient. Pin to the latest 4.14.x line until Starlette
# migrates that annotation to anyio.from_thread.BlockingPortal.
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32
pydantic==2.13.5
requests==2.34.2
psutil==7.2.2
python-dotenv==1.2.3
anyio==4.15.1
